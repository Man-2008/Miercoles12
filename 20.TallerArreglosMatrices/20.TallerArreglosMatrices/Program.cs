using System;
namespace _20.TallerArreglosMatrices
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*Desarrollar un programa que cree una matriz de 10 filas y 20 columnas
             y muestre por pantalla la suma de los elementos de cada columna.

            int [,] matriz = new int[10, 20];
            int[] SumaColumnas = new int[20];

            Console.WriteLine("Ingrese los elementos de la matriz: ");

            for(int i = 0; i < 10; i++)
            {
                for(int j = 0; j < 20; j++)
                {
                    matriz[i,j] = int.Parse(Console.ReadLine());

                }
            }

            for (int j = 0; j < 20; j++)
            {
                for (int i = 0; i < 10; i++)
                {
                    SumaColumnas[j] += matriz[i, j];
                }
                Console.WriteLine($"La suma de la columna {j+1} es: {SumaColumnas[j]}");
            }*/


        }
    }
}
